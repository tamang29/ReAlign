import { SagaIterator } from 'redux-saga';
/**
 * Fixes the layout of the diagram after importing a patch.
 */
export declare function PatchLayouter(): SagaIterator;
export declare function patchLayout(): SagaIterator;
